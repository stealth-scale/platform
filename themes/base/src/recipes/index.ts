/**
 * What this theme draws differently from Chakra, a recipe at a time.
 *
 * A recipe here either corrects something Chakra draws badly or supplies one it forgot. Anything a
 * component of this kit owns outright keeps its recipe beside the component instead; these are for
 * the components Chakra itself owns, which look their recipe up from the system by name and can be
 * reached no other way.
 */

export { accordion } from "#recipes/accordion.ts";
export { inputAddon } from "#recipes/input-addon.ts";
export { kbd } from "#recipes/kbd.ts";
export { menu } from "#recipes/menu.ts";
export { segmentGroup } from "#recipes/segment-group.ts";
export { switchRecipe } from "#recipes/switch.ts";
export { table } from "#recipes/table.ts";
export { tabs } from "#recipes/tabs.ts";
export { timeline } from "#recipes/timeline.ts";
export { toggle } from "#recipes/toggle.ts";
export { treeView } from "#recipes/tree-view.ts";
