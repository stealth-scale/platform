/**
 * What a config extends: the layers a catalogue needs, and the layers a root states for specimens.
 */

export { indexed, layers } from "#preset/catalogue.ts";
export { workspace } from "#preset/workspace.ts";
